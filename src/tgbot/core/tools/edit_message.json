{
	"type": "function",
	"function": {
		"name": "edit_message",
		"description": "Edits the given message to change its text or media.",
		"parameters": {
			"type": "object",
			"properties": {
				"message": {
					"type": "integer",
					"description": "消息ID，可从Telegram消息链接获取, 例如：https://t.me/{chat_id}/{message_id}"
				},
				"text": {
					"type": "string",
					"description": "The new text of the message."
				},
				"link_preview": {
					"type": "boolean",
					"description": "Should the link preview be shown?"
				}
			},
			"required": [
				"message"
			]
		}
	}
}
