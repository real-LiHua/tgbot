{
    "type": "function",
    "function": {
	"name": "pin_message",
	"description": "Pins a message in a chat. The default behaviour is to not notify members",
	"parameters": {
	    "type": "object",
	    "properties": {
		"message": {
		    "type": "integer",
		    "description": "消息ID，可从Telegram消息链接获取, 例如：https://t.me/{chat_id}/{message_id}"
		},
		"notify":{
		    "type": "boolean",
		    "description": "Whether the pin should notify people or not."
		}
	    },
	    "required":["message"]
	}
    }
}
