{
	"type": "function",
	"function": {
		"name": "kick_participant",
		"description": "Kicks a user from a chat. Kicking yourself ('me') will result in leaving the chat.",
		"parameters": {
			"type": "object",
			"properties": {
				"user": {
					"type": "integer",
					"description": "The user to kick."
				}
			},
			"required": [
				"user"
			]
		}
	}
}
