{
    "type": "function",
    "function": {
	"name": "unpin_message",
	"description": "Unpins a message in a chat. If no message ID is specified, all pinned messages will be unpinned.",
	"parameters": {
	    "type": "object",
	    "properties": {
		"message": {
		    "type": "integer",
		    "description": "消息ID，可从Telegram消息链接获取, 例如：https://t.me/{chat_id}/{message_id}"
		}
	    },
	    "required":["message"]
	}
    }
}
