{
	"type": "function",
	"function": {
		"name": "forward_messages",
		"description": "Forwards the given messages to the specified entity.",
		"parameters": {
			"type": "object",
			"properties": {
				"messages": {
					"type": "array",
					"description": "The messages integer IDs."
				},
				"from_peer": {
					"type": "integer",
					"description": ""
				},
				"silent": {
					"type": "boolean",
					"description": "Whether the message should notify people in a broadcast channel or not. Defaults to False, which means it will notify them. Set it to True to alter this behaviour."
				}
			},
			"required": [
				"message"
			]
		}
	}
}
